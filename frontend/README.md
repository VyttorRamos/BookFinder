# BookFinder Frontend

Scaffold React app (Vite) ported from `backend/templates`.

To run:

1. Install dependencies

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend to be available at `http://127.0.0.1:5000` (same as original `main.js`).
